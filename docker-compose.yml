services:
  blog:
    # build: .
    image: volla12345/blogg:latest
    ports:
      - "80:80"
    env_file:
      - app/.env
    volumes:
      - rails_storage:/rails/storage

volumes:
  rails_storage:
